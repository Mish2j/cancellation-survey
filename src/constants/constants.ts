export enum Page {
  SURVEY = "SURVEY",
  MAIN = "MAIN",
}

export enum SurveyOption {
  RECURRING_MEMBERSHIP = "recurring membership",
  BETTER_SOLUTION = "better solution",
  TOO_EXPENSIVE = "too expensive",
  BUGS = "bugs",
  SINGLE_PLUGIN = "single plugin",
  NOT_USING = "not using",
  OTHER = "other",
}
